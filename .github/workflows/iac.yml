name: Deploy Trucking Tracker

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    defaults:
      run:
        working-directory: iac

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    # ----------------------------
    # Azure Login
    # ----------------------------
    - name: Login to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # ----------------------------
    # Create SSH key file
    # ----------------------------
    - name: Create SSH Key
      run: |
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ssh_key.pem
        chmod 600 ssh_key.pem

    # ----------------------------
    # Terraform
    # ----------------------------
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Terraform Init
      run: terraform init -reconfigure

    # ----------------------------
    # Format Check
    # ----------------------------
    - name: Terraform Format
      run: terraform fmt -check

    # ----------------------------
    # Validate
    # ----------------------------
    - name: Terraform Validate
      run: terraform validate

    # ----------------------------
    # Plan
    # ----------------------------
    - name: Terraform Plan
      run: terraform plan -var="my_ip=${{ secrets.MY_IP }}/32" -out=tfplan

    # ----------------------------
    # Apply only on main branch
    # ----------------------------
    - name: Terraform Apply
      if: github.ref == 'refs/heads/main'
      run: terraform apply -auto-approve tfplan

    #  SSH
    - name: Export SSH key
      id: sshkey
      run: |
        terraform output -raw ssh_private_key > id_rsa
        chmod 600 id_rsa

    # GitHub environment dev

#    - name: Save SSH Key as Secret
#      env:
#        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#      run: |
#        gh secret set VM_SSH_KEY \
#          --env dev \
#          --body "$(cat id_rsa)"

#    - name: Export Private Key
#      id: export_key
#      run: |
#        echo "PRIVATE_KEY<<EOF" >> $GITHUB_ENV
#        terraform output -raw private_key_pem >> $GITHUB_ENV
#        echo "EOF" >> $GITHUB_ENV
#
#    - name: Create SSH key file
#      run: |
#        echo "$PRIVATE_KEY" > ssh_key.pem
#        chmod 600 ssh_key.pem

    # ----------------------------
    # Install Ansible
    # ----------------------------
    - name: Install Ansible
      run: sudo apt-get update && sudo apt-get install ansible -y

    # ----------------------------
    # Run Ansible
    # ----------------------------
    - name: Run Ansible
      #run: ansible-playbook -i ansible/hosts.ini ansible/playbook.yml
      run: |
        terraform -chdir=../iac output -raw vm_ip > ip.txt
        echo "[web]" > hosts.ini
        echo "$(cat ip.txt)" >> hosts.ini

        ansible-playbook -i hosts.ini playbook.yml \
          --user azureuser \
          --private-key ../iac/id_rsa      
